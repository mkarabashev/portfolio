import React from 'react';
import DropDownMenu from 'material-ui/DropDownMenu';
import MenuItem from 'material-ui/MenuItem';

const NavMenu = ({ onSelect, selected }) => (
  <DropDownMenu>
    <MenuItem></MenuItem>
  </DropDownMenu>
);

export default NavMenu;
